Install pytmx, pygame_gui

if get_frect is inside the code, change to get_rect or install pygame-ce

uninstall pygame first then install pygame-ce

pip uninstall pygame
pip install pygame-ce
pip install pytmx
pip install pygame_gui
